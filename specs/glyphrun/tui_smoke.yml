version: 1
name: local_agent_tui_smoke

intent: |
  a user can launch the local-agent TUI, see the initial screen, and quit with ctrl+c.

target:
  cmd: ["./bin/local-agent"]
  cwd: "."
  env:
    HOME: "${projectRoot}/.glyphrun/tmp/home"
    LOCAL_AGENT_AGENTS_DIR: "${projectRoot}/.glyphrun/tmp/agents"
    OLLAMA_HOST: "http://127.0.0.1:1"

preconditions:
  commands:
    - run: "rm -rf .glyphrun/tmp/home .glyphrun/tmp/agents && mkdir -p .glyphrun/tmp/home/.config/local-agent .glyphrun/tmp/agents && printf '%s\\n' 'model:' '  auto_select: false' > .glyphrun/tmp/home/.config/local-agent/config.yaml && go build -o ./bin/local-agent ./cmd/local-agent"
      cwd: "."
      timeoutMs: 30000

steps:
  - wait:
      screen:
        contains: "Ask, @mention files, or type /help"
      timeoutMs: 5000
  - wait:
      screen:
        contains: "try: ollama serve"
      timeoutMs: 5000
  - snapshot: launch
  - press: "ctrl+c"
  - wait:
      process:
        exitCode: 0
      timeoutMs: 3000

outcomes:
  - id: launch_snapshot_stable
    description: the normal-width welcome and settled local-runtime guidance remain stable
    verify:
      snapshot:
        name: launch
        mode: text
  - id: welcome_visible
    description: the TUI renders its welcome screen
    verify:
      screen:
        contains: "LOCAL AGENT"
  - id: ctrl_c_quits
    description: ctrl+c exits the TUI cleanly
    verify:
      process:
        exitCode: 0
